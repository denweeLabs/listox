import 'package:flutter/material.dart';

class AccountItemsDivider extends StatelessWidget {
  const AccountItemsDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Divider(height: 8);
  }
}
