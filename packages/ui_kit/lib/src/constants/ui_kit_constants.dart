import 'package:flutter/material.dart';

class UiKitConstants {
  static const commonBorderRadius = BorderRadius.all(Radius.circular(18));
  static const commonDialogBorderRadius = BorderRadius.all(Radius.circular(34));
}
